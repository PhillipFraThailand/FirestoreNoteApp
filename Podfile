# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'noteAppDemoFirestore' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for noteAppDemoFirestore
  pod 'Firebase/Core'
  pod 'Firebase/Firestore'
  pod 'Firebase/Auth'


  target 'noteAppDemoFirestoreTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'noteAppDemoFirestoreUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
